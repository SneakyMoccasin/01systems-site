export const PULSE_SETTINGS = {
  unit: {
    minScale: 0.95,
    maxScale: 1.08,
    speed: 0.003,   // fastest (agents feel alive)
  },
  prop: {
    minScale: 0.97,
    maxScale: 1.03,
    speed: 0.0018,  // slower
  },
  marker: {
    minScale: 0.90,
    maxScale: 1.20,
    speed: 0.005,   // strong pulse to stand out
  },
  effect: {
    minScale: 0.85,
    maxScale: 1.25,
    speed: 0.006, // fastest + strongest pulse
  }
};

